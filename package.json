{
  "name": "bilkent-gamejam",
  "homepage": "https://mrhonneynive.github.io/bilkent-gamejam",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist",
    "dev": "vite",
    "preview": "vite preview",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
  },
  "dependencies": {
    "kaplay": "^3001.0.9"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "gh-pages": "^6.3.0",
    "vite": "^6.1.0"
  }
}
